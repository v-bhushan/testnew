name: Trigger auto deployment for antps14

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ newbranch ]
    paths:
    - '**'
    - '.github/workflows/antps14-AutoDeployTrigger-5c3ec17b-dbf4-4382-a0a5-40cbdb047f3f.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: antuxtest.azurecr.io
          username: ${{ secrets.ANTPS14_REGISTRY_USERNAME }}
          password: ${{ secrets.ANTPS14_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: antuxtest.azurecr.io/antps14:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.ANTPS14_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az config set extension.use_dynamic_install=yes_without_prompt
            az containerapp registry set -n antps14 -g andimarc-ca-newrp-rg --server antuxtest.azurecr.io --username  ${{ secrets.ANTPS14_REGISTRY_USERNAME }} --password ${{ secrets.ANTPS14_REGISTRY_PASSWORD }}
            az containerapp update -n antps14 -g andimarc-ca-newrp-rg --image antuxtest.azurecr.io/antps14:${{ github.sha }}
